apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "k8s-gitops-deployment-fastapi.fullname" . }}-test-connection"
  labels:
    {{- include "k8s-gitops-deployment-fastapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "k8s-gitops-deployment-fastapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
